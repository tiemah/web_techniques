// Write a function that receives a parameter and displays it on the screen

const name = "Doreen";
function display(name){
    console.log(name);
}
display(name); //performs a function call
//The output will be: Doreen

// setTimeout(display(name), 5000); this one does not work appropriately it displays the name immediately instead of the specified time